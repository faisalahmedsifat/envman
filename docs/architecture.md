# Architecture

This document will describe the runtime architecture for `envman`.

Initial focus:

- CLI entrypoint and command dispatch
- repo-local `.envman/` storage layout
- crypto and wrapped-key boundaries
- merge engine boundaries
- filesystem safety and atomic writes
