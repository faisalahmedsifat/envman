# Security Model

This document will define the security model for `envman`.

Initial focus:

- shared repo passphrase assumptions
- passphrase resolution order
- wrapped-key profile format
- passphrase rotation behavior
- session cache boundaries
